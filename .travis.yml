language: python
sudo: false
cache: pip
python:
  - 3.6
  - 3.6-dev
  - 3.7-dev

jobs:
  include:
    - stage: test
      script: make test-ci

    - stage: test coverage
      script: make test-coverage upload-coverage

notifications:
  email:
    on_success: never
