matrix:
  include:
    - language: python
      sudo: false
      python: 3.6
      cache: pip
      install: pip install -r server/requirements.txt
      script: make py-ci
      after_success:
        - if [[ "$TRAVIS_BRANCH" == "master" ]]; then
            make coverage
          fi

    - language: node_js
      node_js: "stable"
      cache:
        yarn: true
        directories:
          - web_app/node_modules
      script: make js-ci

notifications:
  email:
    on_success: never
